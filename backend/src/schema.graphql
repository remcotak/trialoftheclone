# import * from './generated/prisma.graphql'

type Mutation {
  createUser(name: String!): User
  createAdventure(title: String!, act: Int, page: Int): Adventure
}

type Query {
  users: [User]!
  adventures: [Adventure]!
}
