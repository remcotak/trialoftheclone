# import * from './generated/prisma.graphql'

type Mutation {
  createUser(name: String!): User
  createAdventure(title: String!, alive: Boolean): Adventure
}

type Query {
  users: [User]!
  adventures: [Adventure]!
}
